generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model JournalEntry {
  id        String   @id @default(uuid())
  content   String
  mood      String?
  createdAt DateTime @default(now())
}
